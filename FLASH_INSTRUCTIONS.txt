==============================================================================
  OUI SPY UNIFIED BLUE -- Production Flash Instructions
  Boards: XIAO ESP32-S3 (default)  |  XIAO ESP32-C6 (use: flash.py --c6)
  The steps below are for the S3; see "XIAO ESP32-C6 VARIANT" at the bottom.
==============================================================================

  WHAT'S IN THE BOX
  -----------------
  flash.py              Flasher script (Python 3)
  requirements.txt      Python dependencies
  firmware/
    bootloader.bin      ESP32-S3 bootloader         (0x0000)
    partitions.bin      Partition table              (0x8000)
    boot_app0.bin       OTA data                     (0xe000)
    oui-spy-unified-blue.bin  Application firmware   (0x10000)


  SETUP (one time)
  ----------------
  1. Install Python 3.8+ from https://www.python.org
     - Windows: check "Add Python to PATH" during install
     - Linux:   sudo apt install python3 python3-pip
     - macOS:   brew install python3

  2. Install USB drivers if needed:
     - CH340/CH341:  https://www.wch-ic.com/downloads/CH341SER_ZIP.html
     - CP210x:       https://www.silabs.com/developers/usb-to-uart-bridge-vcp-drivers

  3. Install Python dependencies:

     pip install -r requirements.txt


  FLASH ONE BOARD
  ---------------
  1. Plug in XIAO ESP32-S3 via USB-C
  2. Run:

     python flash.py

  3. Confirm when prompted
  4. Wait for "Done!" -- board reboots automatically


  FLASH MANY BOARDS (batch mode)
  ------------------------------
  1. Run:

     python flash.py --batch

  2. Plug in first board, script auto-detects and flashes
  3. When done, swap board and press Enter
  4. Repeat until all boards are flashed
  5. Press Ctrl+C or type 'q' when finished

  The script tracks success/fail counts for you.

  To erase flash before writing (clean slate):

     python flash.py --batch --erase


  VERIFYING A FLASHED BOARD
  -------------------------
  After a successful flash, the board reboots automatically.

  1. Listen for 4 ASCENDING BEEPS -- this is the boot confirmation.
     If you hear the 4 beeps, the firmware is running correctly.
  2. A WiFi AP appears: SSID "oui-spy" / password "ouispy123"
  3. Connect to that AP and open http://192.168.4.1 in a browser
  4. You should see the mode selector dashboard

  Quick batch test cycle:
     Plug in -> auto-flash -> hear 4 beeps -> unplug -> next board


  TROUBLESHOOTING
  ---------------
  "No ESP32 detected"
    - Check USB cable (must be data cable, not charge-only)
    - Install CH340/CP210x drivers (see setup step 2)
    - Try a different USB port
    - On Windows: check Device Manager for COM port

  "esptool not found"
    - Run: pip install esptool pyserial
    - Make sure Python is in your PATH

  Flash succeeds but board doesn't boot
    - Make sure all 4 .bin files are in firmware/ folder
    - Try: python flash.py --erase   (full erase first)

  Multiple boards detected
    - The script lists all ports and lets you pick one
    - Unplug other USB serial devices if possible

==============================================================================


==============================================================================
  XIAO ESP32-C6 VARIANT
==============================================================================

  This repo also builds for the Seeed XIAO ESP32-C6 (single-core, no PSRAM,
  4 MB flash, USB-Serial-JTAG). No prebuilt C6 binaries ship here — the C6
  needs Arduino-ESP32 core 3.x, so build them once with PlatformIO:

     pio run -e seeed_xiao_esp32c6

  Easiest flash path (PlatformIO flashes directly, no flash.py needed):

     pio run -e seeed_xiao_esp32c6 -t upload

  To use this flasher script instead, copy the four build artifacts into
  firmware-c6/ (see firmware-c6/README.txt), then:

     python flash.py --c6

  Offsets are identical to the S3 (bootloader 0x0, partitions 0x8000,
  boot_app0 0xe000, app 0x10000); --c6 just targets chip esp32c6 / 4 MB.

  Wiring differs from the S3 — buzzer on D2 (GPIO2), Detector NeoPixel on D1
  (GPIO1); onboard LED (GPIO15) and BOOT button (GPIO9) need no wiring.
  Full details: PORTING_ESP32C6.md
==============================================================================


==============================================================================
  HELTEC WiFi LoRa 32 V4 VARIANT
==============================================================================

  Also an ESP32-S3 (16 MB flash, 2 MB PSRAM, native USB), so it reuses the
  XIAO-S3 toolchain. Verified binaries are already in firmware-heltec/; rebuild
  them only if you change the firmware:

     pio run -e heltec_wifi_lora_32_v4

  Easiest flash path (PlatformIO flashes directly, no flash.py needed):

     pio run -e heltec_wifi_lora_32_v4 -t upload

  Or use this flasher with the bins in firmware-heltec/:

     python flash.py --heltec

  Offsets are identical to the S3 (bootloader 0x0, partitions 0x8000,
  boot_app0 0xe000, app 0x10000); --heltec just targets chip esp32s3 / 16MB.

  Board notes: no on-board buzzer or NeoPixel (audible/RGB alerts are silent
  unless you wire external parts to GPIO7 / GPIO4); the on-board white LED
  (GPIO35) reads inverted; GPIO21 is the OLED reset (not an LED). All six modes
  are verified running on the hardware. Full pin map: see the
  [env:heltec_wifi_lora_32_v4] block in platformio.ini.
==============================================================================
